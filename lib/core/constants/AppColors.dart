import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ============================================================
  // PRIMARY
  // ============================================================

  static const Color primary = Color(0xFFC6A65D);

  static const Color primaryDark = Color(0xFF8E742F);
  static const Color primaryMedium = Color(0xFFB09246);
  static const Color primaryLight = Color(0xFFE3C77C);
  static const Color primarySoft = Color(0xFFFFE4A8);
  static const Color primaryContainer = Color(0xFFFFF1D2);

  // ============================================================
  // SECONDARY
  // ============================================================

  static const Color secondary = Color(0xFF1A1A1A);

  static const Color secondaryDark = Color(0xFF101010);
  static const Color secondaryMedium = Color(0xFF4D4D4D);
  static const Color secondaryLight = Color(0xFF7A7A7A);
  static const Color secondarySoft = Color(0xFFBDBDBD);

  // ============================================================
  // TERTIARY / ERROR
  // ============================================================

  static const Color tertiary = Color(0xFFD32F2F);

  static const Color tertiaryDark = Color(0xFF8E0000);
  static const Color tertiaryMedium = Color(0xFFB71C1C);
  static const Color tertiaryLight = Color(0xFFFF5252);
  static const Color tertiarySoft = Color(0xFFFF8A80);
  static const Color tertiaryContainer = Color(0xFFFFEBEE);

  // ============================================================
  // NEUTRAL
  // ============================================================

  static const Color neutral = Color(0xFFF5F5F5);

  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceContainer = Color(0xFFEEEEEE);

  static const Color border = Color(0xFFD6D6D6);
  static const Color divider = Color(0xFFE0E0E0);

  // ============================================================
  // TEXT
  // ============================================================

  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF5F5A50);
  static const Color textTertiary = Color(0xFF7D7D7D);

  static const Color textOnPrimary = Color(0xFF1A1A1A);
  static const Color textOnSecondary = Color(0xFFFFFFFF);
  static const Color textOnTertiary = Color(0xFFFFFFFF);

  // ============================================================
  // DISABLED
  // ============================================================

  static const Color disabled = Color(0xFFBDBDBD);

  static const Color disabledBackground = Color(0xFFE0E0E0);

  static const Color disabledBorder = Color(0xFFC7C7C7);

  static const Color disabledText = Color(0xFF9E9E9E);

  static const Color disabledIcon = Color(0xFF9E9E9E);

  // ============================================================
  // STATES
  // ============================================================

  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFF9A825);
  static const Color error = tertiary;
  static const Color info = Color(0xFF1976D2);

  // ============================================================
  // TRANSPARENT / OVERLAY
  // ============================================================

  static const Color transparent = Colors.transparent;

  static const Color overlay = Color(0x66000000);
}